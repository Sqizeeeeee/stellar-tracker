name: Monitoring Configuration

on:
  push:
    branches: [main, develop]
    paths:
      - 'monitoring/**'
      - 'docker-compose.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'monitoring/**'
      - 'docker-compose.yml'

jobs:
  validate-prometheus:
    runs-on: ubuntu-latest
    name: Validate Prometheus Config
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Validate Prometheus configuration
      run: |
        echo "🔍 Validating Prometheus configuration..."
        docker run --rm \
          -v $(pwd)/monitoring/prometheus:/prometheus-config \
          --entrypoint /bin/promtool \
          prom/prometheus:latest \
          check config /prometheus-config/prometheus.yml
        echo "✅ Prometheus config is valid"
    
    - name: Validate alert rules
      run: |
        echo "🔍 Validating Prometheus alert rules..."
        if [ -d monitoring/prometheus/rules ] && ls monitoring/prometheus/rules/*.yml >/dev/null 2>&1; then
          for rule_file in monitoring/prometheus/rules/*.yml; do
            filename=$(basename "$rule_file")
            echo "Checking $filename..."
            docker run --rm \
              -v $(pwd)/monitoring/prometheus:/prometheus-config \
              --entrypoint /bin/promtool \
              prom/prometheus:latest \
              check rules "/prometheus-config/rules/$filename"
          done
          echo "✅ Alert rules are valid"
        else
          echo "ℹ️ No alert rules found, skipping validation"
        fi
  
  validate-promtail:
    runs-on: ubuntu-latest
    name: Validate Promtail Config
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Check Promtail YAML syntax
      run: |
        echo "🔍 Validating Promtail YAML syntax..."
        if command -v yamllint &> /dev/null; then
          yamllint monitoring/promtail/promtail.yml || echo "⚠️ yamllint not available, skipping"
        else
          python3 -c "import yaml; yaml.safe_load(open('monitoring/promtail/promtail.yml'))"
          echo "✅ Promtail YAML is valid"
        fi
  
  validate-docker-compose:
    runs-on: ubuntu-latest
    name: Validate Monitoring Stack
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Validate docker-compose with monitoring profile
      run: |
        echo "🔍 Validating Docker Compose with monitoring profile..."
        docker compose --profile monitoring config --quiet
        echo "✅ Docker Compose configuration is valid"
    
    - name: Check service health definitions
      run: |
        echo "🔍 Checking health check definitions..."
        if grep -q "healthcheck:" docker-compose.yml; then
          echo "✅ Health checks are defined"
        else
          echo "⚠️ Consider adding health checks to services"
        fi
    
    - name: Verify monitoring profile exists
      run: |
        echo "🔍 Verifying monitoring profile..."
        if grep -q 'profiles:.*monitoring' docker-compose.yml; then
          echo "✅ Monitoring profile is configured"
        else
          echo "❌ Monitoring profile not found"
          exit 1
        fi
  
  test-monitoring-stack:
    runs-on: ubuntu-latest
    name: Test Monitoring Stack
    if: github.event_name == 'pull_request'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Start monitoring stack
      run: |
        echo "🚀 Starting monitoring services..."
        docker compose --profile monitoring up -d
        echo "⏳ Waiting for services to be ready..."
        sleep 45
    
    - name: Check Prometheus health
      run: |
        echo "🔍 Checking Prometheus health..."
        for i in {1..10}; do
          if curl -f http://localhost:9090/-/healthy 2>/dev/null; then
            echo "✅ Prometheus is healthy"
            exit 0
          fi
          echo "Attempt $i/10 failed, retrying..."
          sleep 5
        done
        exit 1
    
    - name: Check Grafana health
      run: |
        echo "🔍 Checking Grafana health..."
        for i in {1..10}; do
          if curl -f http://localhost:3000/api/health 2>/dev/null; then
            echo "✅ Grafana is healthy"
            exit 0
          fi
          echo "Attempt $i/10 failed, retrying..."
          sleep 5
        done
        exit 1
    
    - name: Check Loki health
      continue-on-error: true
      run: |
        echo "🔍 Checking Loki health..."
        for i in {1..15}; do
          if curl -f http://localhost:3100/ready 2>/dev/null; then
            echo "✅ Loki is healthy"
            exit 0
          fi
          echo "Attempt $i/15 failed, retrying..."
          sleep 10
        done
        echo "⚠️ Loki health check failed, but continuing (Loki can be slow to start)"
        exit 0
    
    - name: Verify Prometheus targets
      run: |
        echo "🔍 Verifying Prometheus targets..."
        curl -s http://localhost:9090/api/v1/targets | jq '.data.activeTargets | length'
    
    - name: Show service logs on failure
      if: failure()
      run: |
        echo "📋 Showing logs for debugging..."
        docker compose --profile monitoring logs --tail=50 prometheus
        docker compose --profile monitoring logs --tail=50 grafana
        docker compose --profile monitoring logs --tail=50 loki
    
    - name: Stop monitoring stack
      if: always()
      run: |
        docker compose --profile monitoring down -v
  
  monitoring-report:
    runs-on: ubuntu-latest
    name: Monitoring Quality Gate
    needs: [validate-prometheus, validate-promtail, validate-docker-compose]
    if: always()
    
    steps:
    - name: Generate report
      run: |
        echo "# 📊 Monitoring Configuration Report" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "- Prometheus Config: ${{ needs.validate-prometheus.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- Promtail Config: ${{ needs.validate-promtail.result }}" >> $GITHUB_STEP_SUMMARY
        echo "- Docker Compose: ${{ needs.validate-docker-compose.result }}" >> $GITHUB_STEP_SUMMARY
        
        if [ "${{ needs.validate-prometheus.result }}" != "success" ] || \
           [ "${{ needs.validate-promtail.result }}" != "success" ] || \
           [ "${{ needs.validate-docker-compose.result }}" != "success" ]; then
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "❌ **Monitoring configuration has issues**" >> $GITHUB_STEP_SUMMARY
          exit 1
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "✅ **All monitoring checks passed!**" >> $GITHUB_STEP_SUMMARY
