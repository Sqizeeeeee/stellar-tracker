name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Extract version
      id: version
      run: |
        VERSION=${GITHUB_REF#refs/tags/}
        echo "version=${VERSION}" >> $GITHUB_OUTPUT
        echo "version_number=${VERSION#v}" >> $GITHUB_OUTPUT
        echo "repo_lowercase=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Build and push orchestrator
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: |
          ghcr.io/${{ steps.version.outputs.repo_lowercase }}-orchestrator:${{ steps.version.outputs.version }}
          ghcr.io/${{ steps.version.outputs.repo_lowercase }}-orchestrator:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    - name: Build and push collision service
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./collision-service/Dockerfile
        push: true
        tags: |
          ghcr.io/${{ steps.version.outputs.repo_lowercase }}-collision:${{ steps.version.outputs.version }}
          ghcr.io/${{ steps.version.outputs.repo_lowercase }}-collision:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    - name: Generate changelog
      id: changelog
      run: |
        echo "## 🚀 Release ${{ steps.version.outputs.version }}" > CHANGELOG.md
        echo "" >> CHANGELOG.md
        echo "### Docker Images" >> CHANGELOG.md
        echo "" >> CHANGELOG.md
        echo "- \`ghcr.io/${{ steps.version.outputs.repo_lowercase }}-orchestrator:${{ steps.version.outputs.version }}\`" >> CHANGELOG.md
        echo "- \`ghcr.io/${{ steps.version.outputs.repo_lowercase }}-collision:${{ steps.version.outputs.version }}\`" >> CHANGELOG.md
        echo "" >> CHANGELOG.md
        echo "### Changes" >> CHANGELOG.md
        git log --pretty=format:"- %s" $(git describe --tags --abbrev=0 HEAD^)..HEAD >> CHANGELOG.md || echo "- Initial release" >> CHANGELOG.md
    
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        body_path: CHANGELOG.md
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
